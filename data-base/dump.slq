-- Cria o banco (sem firulas)
CREATE DATABASE vortex;
USE vortex ;

-- ======================================================
-- 1. USUÁRIOS
-- ======================================================
CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT,
    nome VARCHAR(100) NOT NULL,
    email VARCHAR(100) NOT NULL,
    senha VARCHAR(255) NOT NULL,
    telefone VARCHAR(20),
    tipo VARCHAR(20) DEFAULT 'personal',
    status VARCHAR(20) DEFAULT 'ativo',
    criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- ======================================================
-- 2. ALUNOS
-- ======================================================
CREATE TABLE alunos (
    id INT PRIMARY KEY AUTO_INCREMENT,
    personal_id INT NOT NULL,
    nome VARCHAR(100) NOT NULL,
    email VARCHAR(100),
    telefone VARCHAR(20),
    objetivo VARCHAR(100),
    status VARCHAR(20) DEFAULT 'ativo',
    criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- ======================================================
-- 3. EXERCÍCIOS
-- ======================================================
CREATE TABLE exercicios (
    id INT PRIMARY KEY AUTO_INCREMENT,
    nome VARCHAR(100) NOT NULL,
    categoria VARCHAR(50),
    descricao TEXT,
    criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- ======================================================
-- 4. TREINOS
-- ======================================================
CREATE TABLE treinos (
    id INT PRIMARY KEY AUTO_INCREMENT,
    aluno_id INT NOT NULL,
    nome VARCHAR(100) NOT NULL,
    criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- ======================================================
-- 5. EXERCÍCIOS DO TREINO
-- ======================================================
CREATE TABLE treino_exercicios (
    id INT PRIMARY KEY AUTO_INCREMENT,
    treino_id INT NOT NULL,
    exercicio_id INT NOT NULL,
    series INT,
    repeticoes INT,
    peso DECIMAL(6,2),
    criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- ======================================================
-- 6. EXECUÇÕES (aluno fez o treino)
-- ======================================================
CREATE TABLE execucoes (
    id INT PRIMARY KEY AUTO_INCREMENT,
    treino_id INT NOT NULL,
    aluno_id INT NOT NULL,
    data_execucao DATE,
    status VARCHAR(20) DEFAULT 'pendente',
    criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- ======================================================
-- 7. PLANOS
-- ======================================================
CREATE TABLE planos (
    id INT PRIMARY KEY AUTO_INCREMENT,
    nome VARCHAR(50) NOT NULL,
    limite_alunos INT NOT NULL,
    preco DECIMAL(10,2) NOT NULL,
    criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- ======================================================
-- 8. LEADS (interessados)
-- ======================================================
CREATE TABLE leads (
    id INT PRIMARY KEY AUTO_INCREMENT,
    nome VARCHAR(100) NOT NULL,
    email VARCHAR(100) NOT NULL,
    status VARCHAR(20) DEFAULT 'novo',
    criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- ======================================================
-- DADOS DE TESTE
-- ======================================================

-- Planos
INSERT INTO planos (nome, limite_alunos, preco) VALUES
('Starter', 10, 29.90),
('Pro', 50, 59.90),
('Studio', 200, 99.90);

-- Usuário admin
INSERT INTO users (nome, email, senha, tipo, status) VALUES
('Administrador', 'admin@vortex.com', '123456', 'admin', 'ativo');

-- Exercícios base
INSERT INTO exercicios (nome, categoria, descricao) VALUES
('Agachamento', 'Pernas', 'Desça flexionando os joelhos'),
('Supino', 'Peito', 'Deitado no banco, empurre a barra'),
('Prancha', 'Abdômen', 'Mantenha o corpo reto'),
('Burpee', 'Cardio', 'Agachamento, prancha e salto'),
('Remada', 'Costas', 'Puxe a barra em direção ao abdômen');

-- Um aluno exemplo
INSERT INTO alunos (personal_id, nome, email, objetivo) VALUES
(1, 'João Silva', 'joao@email.com', 'Hipertrofia');

-- Um treino exemplo
INSERT INTO treinos (aluno_id, nome) VALUES
(1, 'Treino A - Superior');

-- Exercícios do treino
INSERT INTO treino_exercicios (treino_id, exercicio_id, series, repeticoes, peso) VALUES
(1, 1, 4, 12, 20.00),
(1, 2, 4, 10, 30.00),
(1, 3, 3, 30, 0.00);


CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_alunos_personal ON alunos(personal_id);
CREATE INDEX idx_treinos_aluno ON treinos(aluno_id);
